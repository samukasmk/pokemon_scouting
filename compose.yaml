services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: pokemon_scouting:latest
    ports:
      - "5000:5000"
    environment:
      FLASK_APP: app:create_app
      FLASK_ENV: production
    volumes:
      - ./:/app
    restart: unless-stopped
